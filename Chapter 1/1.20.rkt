#lang racket
; Out of laziness I will not type out the entire stack
; But note
; In applicative order will evaluate the operands then apply the operator
; In normal order we will fully expand then reduce
; Note
; The remainder operation
; This is where the difference is really shown